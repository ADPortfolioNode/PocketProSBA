# PDF and file processing
PyPDF2==1.26.0  # Pinned to production version
python-multipart==0.0.9

# Flask and core web server
flask==2.3.3
flask-cors==4.0.0
flask-socketio==5.3.6
gunicorn==21.2.0
eventlet==0.33.3

# Environment and configuration
python-dotenv==1.0.0

# HTTP and networking
requests==2.31.0
urllib3>=1.26.0,<3.0.0

# Core dependencies/utilities
setuptools>=68.0.0
werkzeug==2.3.7
wheel>=0.42.0
click>=8.0.0
itsdangerous>=2.0.0
jinja2>=3.0.0
markupsafe>=2.0.0

# AI, ML, and vector search (ALIGNED WITH PROD)
numpy==1.24.3  # Aligned with production version for consistency
scikit-learn==1.2.2
pandas==2.2.2
chromadb>=0.4.22,<0.5.0  # Pinned to production range
google-generativeai==0.3.2
openai==1.30.1
tiktoken==0.7.0
langchain-google-genai

# Redis (optional, for caching/sessions)
redis==5.0.4
 
# Pydantic v1 for compatibility with chromadb and other libs (ALIGNED WITH PROD)
pydantic<2,>=1  # Pinned to production range for chromadb compatibility

# --- Optional Production Packages (for awareness) ---
# These are in the production environment but may not be needed for all local development.
# Uncomment if you need to test these features locally.
# prometheus-client==0.17.1
# Flask-Limiter==3.5.0
# sentry-sdk==1.29.2
# flask-talisman==1.1.0
# PyJWT==2.8.0


# Redis (optional, for caching/sessions)
redis==5.0.4
 
# Pydantic v1 for compatibility with chromadb and other libs (ALIGNED WITH PROD)
pydantic<2,>=1  # Pinned to production range for chromadb compatibility

# --- Optional Production Packages (for awareness) ---
# These are in the production environment but may not be needed for all local development.
# Uncomment if you need to test these features locally.
# prometheus-client==0.17.1
# Flask-Limiter==3.5.0
# sentry-sdk==1.29.2
# flask-talisman==1.1.0
# PyJWT==2.8.0
